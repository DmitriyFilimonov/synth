# Синтезатор

## Технологии

Node.js

## Компоненты

### Огибающая

>E = e<sup>(log<sub>e</sub>a<sub>min</sub> * (t / t<sub>end</sub>))</sup>

где

- `e` - число Эйлера
- `t` - текущий момент времени (мс),
- `t`<sub>`end`</sub> - желаемая продолжительность огибающей (мс)
- `a`<sub>`min`</sub> - минимальное значение огибающей, по-умолчанию равно `0.001`
- `E` - нормализованное значение огибающей в момент `t` в пределах `(0; 1]`;


### Осциллятор

Расчитывает отклонение от 0 в текущий момент времени:

- t - время в секундах
- f - частота
- a - амплитуда

>y = sin(t * f * 2 * π) * a

## Звук

Генерирует `.wav` файл (44100 Гц, 16 бит, моно) с суммарным сигналом от всех осцилляторов и нормализацией по их количеству.